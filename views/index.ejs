<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link rel="icon" href="/favicon.png">
    <title>TeamMate</title>
</head>
<body>      
        <%- include('header'); %>
        <div id="innerBody">
            <%- include(title.toLowerCase(), {tab: tab}); %>
        </div>
        <%- include('navbar'); %>
      <script>
          if(document.getElementById('nav' + <%- JSON.stringify(tab) %>)){
              document.getElementById('nav' + <%- JSON.stringify(tab) %>).classList.add('active')
          }   
      </script> 
</body>
</html>